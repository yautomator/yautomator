version: 1.0
runtime: nodejs20
build:
  commands:
    pre-build:
      - npm ci
    build:
      - npx svelte-kit sync
      - npm run build
run:
  command: node build/index.js
  network:
    port: 3000
  env:
    - name: NODE_ENV
      value: "production"