version: 1.0
runtime: nodejs14
build:
  commands:
    pre-build:
      - npm install
    build:
      - npm run build
run:
  runtime-version: 14.x
  command: node build
  network:
    port: 3000
  env:
    - name: NODE_ENV
      value: "production"
    - name: PORT
      value: "3000"