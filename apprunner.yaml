version: 1.0
runtime: nodejs20
build:
  commands:
    pre-build:
      - npm install
    build:
      - npm run build
run:
  runtime-version: 20.x
  command: node build
  network:
    port: 3000
  env:
    - name: NODE_ENV
      value: "production"
    - name: PORT
      value: "3000"